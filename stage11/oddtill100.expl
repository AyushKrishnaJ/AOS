int main () {
	decl
		int counter;
	enddecl

	begin
		counter = 0;
		while (counter < 100) do
			if ((counter % 2) != 0) then 
				write(counter);
			endif;
			counter = counter + 1;
		endwhile;
		return 0;
	end
}
